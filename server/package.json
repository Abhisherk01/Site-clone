{
  "name": "meridian-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
      "node": ">=20"
    },
  "scripts": {
    "dev": "node --watch --env-file=.env src/index.js",
    "start": "start": "node --env-file-if-exists=.env src/index.js",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "compression": "^1.8.2",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "prisma": "^6.19.3"
  }
}
