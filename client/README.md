# Meridian

> Premium digital studio website — a single-page marketing site built with React + Vite + Tailwind CSS v4, with a lightweight Express + Prisma backend for contact submissions (in progress).

## Development

```bash
cd client
npm install
npm run dev